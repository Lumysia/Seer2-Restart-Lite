name: Unity CI

run-name: 'Unity CI - ${{ github.event_name }} - ${{ github.ref_name }} - #${{ github.run_number }}'

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
    inputs:
      runTests:
        description: Run tests
        required: false
        default: true
        type: boolean
      buildAndroid:
        description: Build Android
        required: false
        default: false
        type: boolean
      buildWindows:
        description: Build Windows
        required: false
        default: true
        type: boolean
      buildWebGL:
        description: Build WebGL
        required: false
        default: false
        type: boolean
      buildMacOS:
        description: Build macOS
        required: false
        default: false
        type: boolean
      refreshLicenseOnly:
        description: Refresh Unity license only
        required: false
        default: false
        type: boolean

permissions:
  contents: read

defaults:
  run:
    shell: bash

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  UNITY_VERSION: 2022.3.62f2
  ARTIFACT_RETENTION_DAYS: 14

jobs:
  refresh_license:
    name: Refresh Unity license
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.refreshLicenseOnly }}
    permissions:
      actions: read
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          lfs: true
          fetch-depth: 0

      - name: Create directories
        run: mkdir -p artifacts/license

      - name: Create Unity activation file
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_SERIAL: ${{ secrets.UNITY_SERIAL }}
        run: |
          docker run --rm \
            -v "${GITHUB_WORKSPACE}:/project" \
            -w /project \
            -e UNITY_EMAIL \
            -e UNITY_PASSWORD \
            -e UNITY_LICENSE \
            -e UNITY_SERIAL \
            "unityci/editor:ubuntu-${{ env.UNITY_VERSION }}-base-3" \
            bash -lc '
              set -euo pipefail
              mkdir -p artifacts/license
              /opt/unity/Editor/Unity \
                -batchmode \
                -nographics \
                -quit \
                -projectPath /project \
                -createManualActivationFile \
                -logFile /project/artifacts/license/create-alf.log
            '

      - name: Validate activation file
        run: test -f artifacts/license/Unity_v${{ env.UNITY_VERSION }}.alf || test -f Unity_v${{ env.UNITY_VERSION }}.alf

      - name: Normalize activation file path
        run: |
          if [[ -f "Unity_v${{ env.UNITY_VERSION }}.alf" ]]; then
            mv "Unity_v${{ env.UNITY_VERSION }}.alf" "artifacts/license/Unity_v${{ env.UNITY_VERSION }}.alf"
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v4

      - name: Install license tools
        run: npm install --global unity-license-activate unity-verify-code

      - name: Activate Unity license
        env:
          UNITY_TOTP_KEY: ${{ secrets.UNITY_TOTP_KEY }}
        run: |
          command=(unity-license-activate "${{ secrets.UNITY_EMAIL }}" "${{ secrets.UNITY_PASSWORD }}" "artifacts/license/Unity_v${{ env.UNITY_VERSION }}.alf")

          if [[ -n "${UNITY_TOTP_KEY:-}" ]]; then
            command+=(--authenticator-key "$UNITY_TOTP_KEY")
          fi

          "${command[@]}"

      - name: Read Unity license content
        id: license_content
        run: |
          license_file=$(find . -type f -name '*.ulf' -print -quit)
          test -n "$license_file"
          {
            echo 'content<<EOF'
            cat "$license_file"
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Detect access token availability
        id: access_token
        env:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        run: |
          if [[ -n "${ACCESS_TOKEN:-}" ]]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Update UNITY_LICENSE with access token
        id: update_with_access_token
        if: ${{ steps.access_token.outputs.available == 'true' }}
        uses: hmanzur/actions-set-secret@v2.0.0
        with:
          name: UNITY_LICENSE
          value: ${{ steps.license_content.outputs.content }}
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Update UNITY_LICENSE with GitHub App token
        id: update_with_app_token
        if: ${{ always() && steps.update_with_access_token.outcome == 'failure' && github.token != '' }}
        uses: hmanzur/actions-set-secret@v2.0.0
        with:
          name: UNITY_LICENSE
          value: ${{ steps.license_content.outputs.content }}
          token: ${{ github.token }}

      - name: Validate secret update
        if: ${{ steps.update_with_access_token.outcome != 'success' && steps.update_with_app_token.outcome != 'success' }}
        run: |
          echo "Failed to update UNITY_LICENSE with ACCESS_TOKEN and github.token" >&2
          exit 1

  test:
    name: Test ${{ matrix.testMode }}
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.runTests }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - testMode: EditMode
            cacheScope: test-editmode
          - testMode: PlayMode
            cacheScope: test-playmode
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          lfs: true
          fetch-depth: 0

      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-${{ matrix.cacheScope }}-${{ env.UNITY_VERSION }}-${{ hashFiles('Packages/manifest.json', 'Packages/packages-lock.json', 'ProjectSettings/ProjectVersion.txt') }}
          restore-keys: |
            Library-${{ matrix.cacheScope }}-${{ env.UNITY_VERSION }}-
            Library-${{ matrix.cacheScope }}-

      - name: Run Unity tests
        uses: game-ci/unity-test-runner@v4
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
          UNITY_SERIAL: ${{ secrets.UNITY_SERIAL }}
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        with:
          projectPath: .
          unityVersion: ${{ env.UNITY_VERSION }}
          testMode: ${{ matrix.testMode }}
          artifactsPath: artifacts/test-results/${{ matrix.testMode }}

      - name: Upload test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.testMode }}
          path: artifacts/test-results/${{ matrix.testMode }}
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}
          if-no-files-found: ignore

      - name: Upload test logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-logs-${{ matrix.testMode }}
          path: |
            artifacts/test-results/${{ matrix.testMode }}/**/*.log
            artifacts/test-results/${{ matrix.testMode }}/**/TestResults.xml
            artifacts/test-results/${{ matrix.testMode }}/**/test-results.xml
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}
          if-no-files-found: ignore

  build_android:
    name: Build Android
    needs:
      - test
    if: ${{ always() && github.event_name == 'workflow_dispatch' && inputs.buildAndroid && (needs.test.result == 'success' || needs.test.result == 'skipped') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          lfs: true
          fetch-depth: 0

      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-build-android-${{ env.UNITY_VERSION }}-${{ hashFiles('Packages/manifest.json', 'Packages/packages-lock.json', 'ProjectSettings/ProjectVersion.txt') }}
          restore-keys: |
            Library-build-android-${{ env.UNITY_VERSION }}-
            Library-build-android-

      - name: Build Android player
        uses: game-ci/unity-builder@v4
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
          UNITY_SERIAL: ${{ secrets.UNITY_SERIAL }}
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        with:
          projectPath: .
          unityVersion: ${{ env.UNITY_VERSION }}
          targetPlatform: Android
          buildsPath: build

      - name: Upload Android artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-android
          path: build/Android
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}
          if-no-files-found: error

      - name: Upload Android logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-android
          path: |
            build/Android/**/*.log
            build/Android/**/output.log
            build/Android/**/UnityPlayer*.txt
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}
          if-no-files-found: ignore

  build_windows:
    name: Build Windows
    needs:
      - test
    if: ${{ always() && (needs.test.result == 'success' || needs.test.result == 'skipped') && (github.event_name != 'workflow_dispatch' || inputs.buildWindows) }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          lfs: true
          fetch-depth: 0

      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-build-windows-${{ env.UNITY_VERSION }}-${{ hashFiles('Packages/manifest.json', 'Packages/packages-lock.json', 'ProjectSettings/ProjectVersion.txt') }}
          restore-keys: |
            Library-build-windows-${{ env.UNITY_VERSION }}-
            Library-build-windows-

      - name: Build Windows player
        uses: game-ci/unity-builder@v4
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
          UNITY_SERIAL: ${{ secrets.UNITY_SERIAL }}
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        with:
          projectPath: .
          unityVersion: ${{ env.UNITY_VERSION }}
          targetPlatform: StandaloneWindows64
          buildsPath: build

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-windows
          path: build/StandaloneWindows64
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}
          if-no-files-found: error

      - name: Upload Windows logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-windows
          path: |
            build/StandaloneWindows64/**/*.log
            build/StandaloneWindows64/**/output.log
            build/StandaloneWindows64/**/UnityPlayer*.txt
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}
          if-no-files-found: ignore

  build_webgl:
    name: Build WebGL
    needs:
      - test
    if: ${{ always() && github.event_name == 'workflow_dispatch' && inputs.buildWebGL && (needs.test.result == 'success' || needs.test.result == 'skipped') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          lfs: true
          fetch-depth: 0

      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-build-webgl-${{ env.UNITY_VERSION }}-${{ hashFiles('Packages/manifest.json', 'Packages/packages-lock.json', 'ProjectSettings/ProjectVersion.txt') }}
          restore-keys: |
            Library-build-webgl-${{ env.UNITY_VERSION }}-
            Library-build-webgl-

      - name: Build WebGL player
        uses: game-ci/unity-builder@v4
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
          UNITY_SERIAL: ${{ secrets.UNITY_SERIAL }}
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        with:
          projectPath: .
          unityVersion: ${{ env.UNITY_VERSION }}
          targetPlatform: WebGL
          buildsPath: build

      - name: Upload WebGL artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-webgl
          path: build/WebGL
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}
          if-no-files-found: error

      - name: Upload WebGL logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-webgl
          path: |
            build/WebGL/**/*.log
            build/WebGL/**/output.log
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}
          if-no-files-found: ignore

  build_macos:
    name: Build macOS
    needs:
      - test
    if: ${{ always() && github.event_name == 'workflow_dispatch' && inputs.buildMacOS && (needs.test.result == 'success' || needs.test.result == 'skipped') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          lfs: true
          fetch-depth: 0

      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-build-macos-${{ env.UNITY_VERSION }}-${{ hashFiles('Packages/manifest.json', 'Packages/packages-lock.json', 'ProjectSettings/ProjectVersion.txt') }}
          restore-keys: |
            Library-build-macos-${{ env.UNITY_VERSION }}-
            Library-build-macos-

      - name: Build macOS player
        uses: game-ci/unity-builder@v4
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
          UNITY_SERIAL: ${{ secrets.UNITY_SERIAL }}
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        with:
          projectPath: .
          unityVersion: ${{ env.UNITY_VERSION }}
          targetPlatform: StandaloneOSX
          buildsPath: build

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-macos
          path: build/StandaloneOSX
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}
          if-no-files-found: error

      - name: Upload macOS logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-macos
          path: |
            build/StandaloneOSX/**/*.log
            build/StandaloneOSX/**/output.log
            build/StandaloneOSX/**/UnityPlayer*.txt
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}
          if-no-files-found: ignore
